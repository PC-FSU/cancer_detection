# cancer_detection

## Workflows

1. Update config.yaml<br>
`save all the configuration for the project.`
2. Update secrets.yaml [Optional]<br>
`To save private API keys.`
3. Update params.yaml<br>
`To save model related parameter`
4. Update the entity<br>
` TO ADD`
5. Update the configuration manager in src config
6. Update the components<br>
`Thing like data ingestion, model training`
7. Update the pipeline <br>
`The full training file`
8. Update the main.py<br>
`Where we will define the flask app`
9. Update the dvc.yaml<br>
`dvc is written at the very end, when the full pipeline is done`


